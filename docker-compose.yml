services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - APP_CONFIG=/data/config.yaml
  web:
    build: ./web
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE=http://api:8000
    depends_on:
      - api
